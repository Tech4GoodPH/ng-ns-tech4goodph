import { Photo } from '../interfaces/photo.interface';

// populate this array to be added to MockDataService.generatePhotosArray
// currently not used, MockDataService also needs to be updated if this will be used
export const mockPhotosArray: Photo[] = [];
